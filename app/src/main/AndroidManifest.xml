<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.doantotnghiep">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoanTotNghiep">
        <activity android:name=".Main.MainOder"></activity>
        <activity android:name=".Main.MainCart"></activity>
        <activity android:name=".Main.Login.DangKy"></activity>
        <activity android:name=".Main.Chitietsanpham" />
        <activity android:name=".Home" /> <!-- <activity android:name="com.facebook.CustomTabActivity" -->
        <!-- android:exported="true"> <intent-filter> -->
        <!-- <action android:name="android.intent.action.VIEW" /> -->
        <!-- <category android:name="android.intent.category.DEFAULT" /> -->
        <!-- <category android:name="android.intent.category.BROWSABLE" /> -->
        <!-- <data android:scheme="@string/fb_login_protocol_scheme" -->
        <!-- tools:ignore="AppLinkUrlError" /> -->
        <!-- </intent-filter> </activity> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".Main.Login.Login" />
        <activity android:name=".MainWait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>